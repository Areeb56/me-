{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd apps/web && pnpm install --frozen-lockfile --filter @aios/web... && pnpm --filter @aios/web... run build"
  },
  "deploy": {
    "startCommand": "cd apps/web && pnpm start",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
